{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "PDA MATE-NEM (v4)",
  "description": "Estructura modular de un Proceso de Desarrollo de Aprendizaje (PDA) de Matemáticas, NEM. Flujo: problematización (gancho de contexto real) → 4 subtemas de menor a mayor dificultad, cada uno con su propia mini-actividad calificada de 5 reactivos (mini-resultado independiente con puntos y estrellas) → resultado global del PDA (suma de los 4 mini-resultados).",
  "type": "object",
  "required": ["id", "grado", "campoFormativo", "eje", "titulo", "problematizacion", "subtemas"],
  "properties": {
    "id": {
      "type": "string",
      "description": "Identificador único, convención: '<grado>S-B<bloque>-PDA<num>', ej. '2S-B1-PDA03'"
    },
    "grado": {
      "type": "string",
      "enum": ["1°", "2°", "3°", "Ejercítate"],
      "description": "'Ejercítate' identifica un tema de práctica libre (pseudo-grado): no pertenece a la ruta curricular de ningún grado en particular, vive en data/ejercitate/ en vez de data/grado-N/, y requiere además el campo 'categoria'."
    },
    "trimestre": { "type": "string", "enum": ["1", "2", "3"] },
    "numero": {
      "type": "integer",
      "description": "Número de tema dentro de su grado (1, 2, 3…), en el orden en que se recorren los PDAs de ese grado. Se muestra en la interfaz como 'Tema N'. Para 'Ejercítate', es el número 1-36 de la lista del usuario."
    },
    "categoria": {
      "type": "string",
      "enum": ["basico", "intermedio", "avanzado", "estadistica"],
      "description": "Solo para grado='Ejercítate': agrupa el tema en uno de los 4 caminos de la pantalla de Ejercítate (básicos/intermedios/avanzados/estadística y probabilidad)."
    },
    "campoFormativo": { "type": "string", "const": "Saberes y Pensamiento Científico" },
    "eje": { "type": "string" },
    "titulo": { "type": "string" },
    "contenido": { "type": "string", "description": "Enunciado oficial del Contenido de la NEM" },
    "pda": { "type": "string", "description": "Enunciado oficial del Proceso de Desarrollo de Aprendizaje" },

    "problematizacion": {
      "type": "object",
      "description": "Fase 1: gancho de contexto real que despierta interés, antes de explicar el tema.",
      "required": ["contexto", "pregunta"],
      "properties": {
        "contexto": { "type": "string" },
        "pregunta": { "type": "string" }
      }
    },

    "subtemas": {
      "type": "array",
      "description": "El tema explicado y evaluado en un mínimo de 4 partes 'núcleo', de menor a mayor dificultad (Introductorio/Intermedio/Avanzado/Síntesis). Cada subtema es una mini-actividad autocontenida: teoría (titulo/explicacion/ejemplos) + su propia mini-actividad calificada de 5 reactivos, con resultado (puntos, estrellas) independiente de los demás. Al completar todos, el PDA muestra un resultado global (suma de todos los mini-resultados). Opcionalmente puede haber hasta 3 subtemas adicionales de REPASO después de los 4 núcleo (índice ≥ 4, numerados <N>.5..<N>.7) — no introducen contenido nuevo, repasan lo ya visto en los 4 anteriores, pero se califican y cuentan para el resultado global igual que cualquier otro subtema (ver Paso 13 en la documentación del proyecto).",
      "minItems": 4,
      "maxItems": 7,
      "items": {
        "type": "object",
        "required": ["titulo", "explicacion", "ejemplos", "puntosPorReactivo", "estrellasMax", "reactivos"],
        "properties": {
          "numero": {
            "type": "string",
            "description": "Número jerárquico del subtema, formato '<numero del PDA>.<consecutivo>' (ej. '3.2' = subtema 2 del tema 3, '3.4' = el más difícil/de síntesis de ese tema). Si el PDA tiene subtemas de repaso opcionales, continúan la numeración a partir de .5 (ej. '3.5', '3.6', '3.7'). Se muestra en la interfaz junto al título del subtema."
          },
          "titulo": { "type": "string" },
          "explicacion": { "type": "string", "description": "Explicación del subtema, más desarrollada que una síntesis breve. El subtema 4 ('Síntesis y aplicación') recapitula las ideas clave de los subtemas 1-3." },
          "formula": { "type": "string" },
          "ejemplos": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string" },
            "description": "Uno o más ejemplos resueltos que ilustran el subtema."
          },
          "puntosPorReactivo": { "type": "integer", "description": "Puntos otorgados por cada reactivo correcto de ESTE subtema (mini-actividad independiente)." },
          "estrellasMax": { "type": "integer", "default": 3, "description": "Estrellas máximas de ESTE subtema (mini-actividad independiente)." },
          "reactivos": {
            "type": "array",
            "minItems": 5,
            "maxItems": 5,
            "items": { "$ref": "#/definitions/pregunta" },
            "description": "Los 5 reactivos calificados de este subtema (nivel de dificultad creciente del subtema 1 al 4 dentro del mismo PDA)."
          }
        }
      }
    },

    "practicaExtra": {
      "type": "array",
      "description": "Fase opcional, no calificada: reactivos adicionales mostrados después del resultado global del PDA, para quien quiera seguir practicando el mismo tema. No afectan el puntaje ni las estrellas.",
      "items": { "$ref": "#/definitions/pregunta" }
    }
  },

  "definitions": {
    "pregunta": {
      "type": "object",
      "description": "Una pregunta de cualquiera de los 4 tipos soportados. Los campos requeridos varían según 'tipo'.",
      "required": ["tipo", "retroalimentacion"],
      "properties": {
        "tipo": {
          "type": "string",
          "enum": ["opcion_multiple", "verdadero_falso", "llenar_frase", "relacionar_columnas"]
        },
        "retroalimentacion": { "type": "string", "description": "Explicación mostrada después de responder, sea correcta o incorrecta." },

        "pregunta": { "type": "string", "description": "[opcion_multiple] El enunciado de la pregunta." },
        "opciones": { "type": "array", "items": { "type": "string" }, "description": "[opcion_multiple] Lista de opciones." },
        "respuestaCorrecta": {
          "description": "[opcion_multiple] índice (entero, base 0) de la opción correcta. [verdadero_falso] booleano. [llenar_frase] string esperado."
        },

        "enunciado": { "type": "string", "description": "[verdadero_falso] La afirmación a evaluar." },

        "frase": { "type": "string", "description": "[llenar_frase] Frase con un solo hueco marcado como '___'." },

        "instruccion": { "type": "string", "description": "[relacionar_columnas] Instrucción para el alumno." },
        "columnaA": { "type": "array", "items": { "type": "string" }, "description": "[relacionar_columnas] Elementos a relacionar." },
        "columnaB": { "type": "array", "items": { "type": "string" }, "description": "[relacionar_columnas] Opciones de pareja (se muestran en un <select> por fila)." },
        "parejasCorrectas": {
          "type": "array",
          "items": { "type": "integer" },
          "description": "[relacionar_columnas] Mismo orden que columnaA; cada valor es el índice en columnaB de su pareja correcta."
        }
      }
    }
  }
}
